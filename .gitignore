# Node.js and npm/yarn related files (applies to both client and server)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp
.pnp.js

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local # Catches any other custom local .env files

# Build and distribution directories
# For React projects (e.g., Create React App):
build/
# For Node.js backend if it has a build step (e.g., TypeScript output):
dist/
out/

# Dependency caches
jspm_packages/
typings/

# Test and coverage reports
coverage/
.nyc_output/ # Istanbul code coverage
.jest/

# IDE and OS specific files
.DS_Store # macOS specific
.vscode/ # VS Code settings
.idea/ # WebStorm/IntelliJ IDEA settings
*.swp # Vim swap files
*.swo

# Misc temporary files and logs
*.log # General log files
.cache/ # General cache files
tmp/
*.tmp
*.bak
*.orig

# If you have specific local development database files (e.g., SQLite)
# db.sqlite
# data/*.db

# Next.js specific (if you decide to use Next.js for React)
.next/

# Parcel bundler cache (if using Parcel)
.parcel-cache/

# SvelteKit specific (if you decide to use SvelteKit)
.svelte-kit/

# Docker specific (if you use Docker for local dev or deployment)
# .dockerignore # Only uncomment if you explicitly want to ignore your .dockerignore itself